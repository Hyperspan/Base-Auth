name: Build and Test

on:
  push:
    branches:
      - master
      - Dev
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Secreate
      run: echo ${{ secrets.NUGET_API_KEY }}

    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build
      run: pwd && dotnet build --configuration Release

    - name: Test
      run: dotnet test
    

    
